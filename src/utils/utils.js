const messages = {
    userExists: "User already exists!",
    missingFields: "Missing fields in the request body",
    userIdNotFound: "A user with that ID does not exist",
    postTitleAlreadyExists: 'A post with the provided title already exist'
}

module.exports = {
    messages
}